#include "serialize.h"
#include "../authentication/authentication.h"

void serializeCredentials(credentials *input, struct Buffer *output) {
	
	// serialize_int(x->n, output);
	// serialize_int(x->m, output);
    	// serialize_string(x->string, output);
}

